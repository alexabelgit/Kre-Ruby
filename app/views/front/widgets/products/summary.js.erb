var container = jQuery('[data-hc="product-summary"][data-hc-id="<%= @product.id_from_provider %>"]');
container.html('<%= escape_javascript(render 'front/widgets/products/summary', product: @product, store: @store) %>');
container.remotify().hc_scroll_to();
